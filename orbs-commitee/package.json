{
  "name": "orbs-commitee",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ orbs-commitee",
    "create-local": "graph create --node http://localhost:8020/ orbs-commitee",
    "remove-local": "graph remove --node http://localhost:8020/ orbs-commitee",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 orbs-commitee",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.3",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
